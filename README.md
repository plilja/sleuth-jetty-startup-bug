# sleuth-jetty-startup-bug

Start the application by running DemoApplication.java and it should fail on startup.

Alternatively from command line:
mvn clean install
java -jar target/demo-0.0.1-SNAPSHOT.jar
